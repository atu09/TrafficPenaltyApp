<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trafficpenaltyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ELicenseActivity"></activity>
        <activity android:name=".CheckNotificationActivity" />
        <activity android:name=".PenaltyDetailsActivity" />
        <activity android:name=".CheckPenaltyActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PoliceHomeActivity"
            android:label="@string/title_activity_police_home" />
        <activity
            android:name=".UserhomeActivity"
            android:label="@string/title_activity_userhome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".police_verification" />
        <activity android:name=".policelogin" />
        <activity
            android:name=".ResetpasswordActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".VerificationActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".ForgotpasswordActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".signup"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Fullscreen">

        </activity>
    </application>

</manifest>